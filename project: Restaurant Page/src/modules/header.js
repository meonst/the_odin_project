let return_header = function() {
    let header = document.createElement("div");
    header.setAttribute("id", "header");
    header.textContent = "Salad Bar"
    return header
}

export default return_header